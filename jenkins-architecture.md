jenkins is a CI tool used to automate build, test, and deployment
a. Jenkins Architecture Overview
b. Jenkins Controller (Master)
c. Jenkins Agents (Slaves)
d. Jenkins with Cloud Platforms

AWS: Jenkins controller on EC2, agents on EC2 or autoscaling groups
Azure: Jenkins on Azure VM, agents on VM scale sets
GCP: Jenkins on Compute Engine, agents on managed instances

